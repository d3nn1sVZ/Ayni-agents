# MoneyDevKit credentials. Generate via:
#   npx @moneydevkit/create
# Or create an account manually at moneydevkit.com.
#
# After authentication, copy this file to .env.local and fill in real values.
# .env.local is gitignored — do NOT commit credentials.

MDK_ACCESS_TOKEN=your_api_key_here
MDK_MNEMONIC=your_mnemonic_here
